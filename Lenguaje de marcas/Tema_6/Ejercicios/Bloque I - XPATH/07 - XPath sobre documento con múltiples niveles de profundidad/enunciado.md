# Ejercicio 07: Árbol multinivel y ejes avanzados
Analiza el documento `pedido.xml` y obtén:

- **a)** La ciudad donde vive el cliente.
- **b)** El atributo `ref` del producto llamado "Monitor".
- **c)** ¿Cómo se obtendría el importe total del pedido en XPath 1.0 vs 2.0?
- **d)** Seleccionar las líneas de pedido que están antes de la línea con `num="2"`.
- **e)** Desde cualquier nodo `<linea>`, obtener el `id` del `<pedido>` al que pertenece usando el eje `ancestor`.
