# Ejercicio 11: Uso de variables ($)
Analiza el archivo `precios.xsl`. Intenta calcular el precio final con IVA pero contiene tres errores técnicos relacionados con la declaración y el uso de variables/parámetros.

**Tarea:**
- Localiza los errores (pista: fíjate en cómo se referencian las variables en XPath).
- Corrige la hoja para que el cálculo `base * (1 + iva)` funcione correctamente.
