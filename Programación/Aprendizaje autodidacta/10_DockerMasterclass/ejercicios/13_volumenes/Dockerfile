# ============================================================
#  EJERCICIO 13 - Volumenes y persistencia (imagen: masterclass/ej13)
#
#  La capa de escritura de un contenedor MUERE con el. Para que los datos
#  sobrevivan se montan VOLUMENES en una ruta. La instruccion VOLUME documenta
#  (y crea un punto de montaje para) esa ruta de datos persistentes.
# ============================================================

# TODO 1: Parte de una imagen base de Linux minima (alpine).

# TODO 2: Copia el script app/contador.sh a /contador.sh y dale permisos de
#         ejecucion.

# TODO 3: Declara la ruta  /data  como VOLUMEN de la imagen (ahi escribe el
#         contador su estado persistente).

# TODO 4: Haz que el contenedor ejecute /contador.sh como proceso principal
#         (ENTRYPOINT o CMD en forma de lista).
