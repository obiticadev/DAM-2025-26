# ============================================================
#  EJERCICIO 32 - Variables con .env  (proyecto: masterclass/ej32)
#
#  Compose lee automaticamente un fichero  .env  del directorio del proyecto y
#  sustituye las referencias  ${VARIABLE}  dentro de este compose.yaml. Asi la
#  configuracion (puertos, valores) sale del codigo y vive en un fichero aparte
#  que NO se versiona. El  .env  ya esta dado: tu cableas las  ${...} .
# ============================================================

services:
  web:
    image: nginx:alpine
    # TODO 1: publica el puerto 80 del contenedor en el puerto del host indicado
    #         por la variable  ${WEB_PORT}  (formato  "${WEB_PORT}:80").

    environment:
      # TODO 2: define la variable de entorno  MENSAJE  tomando su valor de
      #         ${MENSAJE}  (interpolacion desde el .env).
