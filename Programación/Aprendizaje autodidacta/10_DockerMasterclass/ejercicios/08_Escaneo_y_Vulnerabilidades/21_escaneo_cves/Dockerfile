# ============================================================
#  EJERCICIO 21 - Escaneo de vulnerabilidades y endurecimiento
#  (imagen: masterclass/ej21)
#
#  Una imagen segura parte de una base RECIENTE y PARCHEADA, aplica las
#  actualizaciones de seguridad disponibles y NO corre como root. Despues se
#  escanea con docker scout (o trivy) para detectar CVEs conocidas.
# ============================================================

# TODO 1: Parte de una base reciente y ligera con TAG explicito: alpine:3.20

# TODO 2: Aplica los parches de seguridad disponibles del sistema base
#         (en alpine: apk upgrade sin dejar cache, --no-cache).

# TODO 3: Crea un usuario sin privilegios llamado  appuser  y conmuta a el
#         (USER) para no ejecutar como root.

# TODO 4: Comando por defecto: imprimir  imagen endurecida  (forma de lista).
