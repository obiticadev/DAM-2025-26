# ============================================================
#  EJERCICIO 22 - Redes y DNS interno (imagen: masterclass/ej22)
#
#  En una RED DEFINIDA POR EL USUARIO, Docker incluye un DNS interno: un
#  contenedor puede llamar a otro por su NOMBRE (no hace falta saber su IP).
#  Esta imagen es un "cliente" que intenta conectarse por HTTP a un host cuyo
#  nombre recibe como argumento. El runner la lanzara en la misma red que un
#  servidor llamado  api  y comprobara que lo resuelve por nombre.
# ============================================================

# TODO 1: Parte de una base ligera con tag explicito: alpine:3.20

# TODO 2: Instala el cliente HTTP  curl  sin dejar cache (apk add --no-cache).

# TODO 3: Copia  app/probe.sh  a  /probe.sh  y dale permisos de ejecucion.

# TODO 4: Declara  /probe.sh  como ENTRYPOINT (forma de lista) y deja como
#         argumento por defecto (CMD)  api .
