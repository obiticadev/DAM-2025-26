# ============================================================
#  EJERCICIO 06 - EXPOSE y publicacion de puertos (imagen: masterclass/ej06)
#
#  EXPOSE documenta que puerto escucha el proceso DENTRO del contenedor.
#  Publicar al host (mapear) se hace al ejecutar con  -p HOST:CONTENEDOR.
# ============================================================

# TODO 1: Parte de la imagen oficial de nginx en su variante alpine (ligera).
#         Esta imagen ya trae el servidor web y su comando de arranque.

# TODO 2: Copia el fichero app/index.html dentro de la imagen, en la carpeta
#         raiz que nginx sirve por defecto: /usr/share/nginx/html/

# TODO 3: Documenta, mediante la instruccion adecuada, que el contenedor
#         escucha en el puerto 80 (es el puerto interno de nginx).

# TODO 4: Añade una etiqueta de metadatos (LABEL) con la clave "ejercicio"
#         y el valor exacto: 06-expose-puertos

# (No necesitas definir CMD: la imagen base de nginx ya arranca el servidor.)
