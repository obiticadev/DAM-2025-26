# ============================================================
#  EJERCICIO 05 - ENV y ARG (imagen: masterclass/ej05)
#
#  ARG  -> variable de BUILD-TIME (solo existe mientras se construye).
#  ENV  -> variable de RUNTIME (vive dentro del contenedor en ejecucion).
# ============================================================

# TODO 1: Parte de una imagen oficial con Python 3.12 en variante ligera.

# TODO 2: Declara un ARG llamado APP_VERSION con valor por defecto 1.0.0
#         (debe tener default para que el build funcione sin --build-arg).

# TODO 3: Crea una variable de entorno (ENV) APP_VERSION cuyo valor sea el del
#         ARG anterior, de forma que persista dentro del contenedor en runtime.

# TODO 4: Crea una variable de entorno (ENV) APP_ENV con el valor exacto:
#         produccion

# TODO 5: Copia app/config.py a /config.py y haz que sea el comando por defecto
#         del contenedor (ejecutado con Python, en forma de lista).
