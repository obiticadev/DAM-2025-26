# ============================================================
#  EJERCICIO 02 - Tu primer Dockerfile real (imagen: masterclass/ej02)
# ============================================================

# TODO 1: Parte de una imagen base OFICIAL que ya traiga el interprete de
#         Python 3.12 instalado, en su variante LIGERA (no la full).

# TODO 2: Define una variable de entorno que evite que Python almacene en
#         buffer su salida (para que los print aparezcan al instante en los
#         logs del contenedor). La variable es PYTHONUNBUFFERED con valor 1.

# TODO 3: Copia el codigo ya resuelto app/saluda.py dentro de la imagen,
#         en la ruta /saluda.py

# TODO 4: Haz que, al arrancar el contenedor sin argumentos, se ejecute ese
#         script con el interprete de Python. Usa la forma de lista (exec form).
