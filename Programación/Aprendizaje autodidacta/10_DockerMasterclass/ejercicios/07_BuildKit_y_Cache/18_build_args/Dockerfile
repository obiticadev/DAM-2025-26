# ============================================================
#  EJERCICIO 18 - Build args para versiones (imagen: masterclass/ej18)
#
#  ARG parametriza el build. Un ARG declarado ANTES del primer FROM puede usarse
#  para elegir la VERSION de la imagen base. Un ARG declarado DESPUES del FROM
#  vive dentro de esa etapa (recuerda: un ARG no cruza la frontera de un FROM).
# ============================================================

# TODO 1: Declara, ANTES del FROM, un ARG llamado PY_VERSION con valor por
#         defecto 3.12

# TODO 2: Usa ese ARG en el FROM para elegir la base:
#         python:${PY_VERSION}-alpine

# TODO 3: Declara, DESPUES del FROM, un ARG APP_VERSION con valor por defecto
#         2.0.0 y conviertelo en una variable de entorno ENV APP_VERSION.

# TODO 4: Copia app/version.py a /version.py.

# TODO 5: Comando por defecto: ejecutar version.py con Python (forma de lista).
