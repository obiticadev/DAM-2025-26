# ============================================================
#  EJERCICIO 08 - Este Dockerfile YA ESTA RESUELTO.
#  Tu reto es escribir el fichero  .dockerignore  (al lado de este).
#  Copia TODO el contexto a /app; lo que no quieras dentro, exclúyelo
#  desde .dockerignore (no tocando este Dockerfile).
# ============================================================
FROM python:3.12-slim
WORKDIR /app
COPY . /app
CMD ["python", "servidor.py"]
