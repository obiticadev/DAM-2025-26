# ============================================================
#  EJERCICIO 20 - Versionado semantico y tags (imagen: masterclass/ej20)
#
#  Una MISMA imagen (mismo ID) puede tener varios tags: 1.4.2, 1.4, 1, latest.
#  El versionado semantico MAJOR.MINOR.PATCH permite rollback y trazabilidad.
#  Aqui dejas la version "grabada" en la imagen y el runner comprobara que se
#  puede re-etiquetar conservando el mismo ID.
# ============================================================

# TODO 1: Parte de una imagen base de Linux minima (alpine).

# TODO 2: Graba la version de la imagen en la etiqueta OCI estandar
#         org.opencontainers.image.version  con el valor exacto  1.4.2

# TODO 3: Pon tambien el titulo OCI org.opencontainers.image.title con valor
#         masterclass-ej20

# TODO 4: Comando por defecto: imprimir la version  1.4.2  (forma de lista).
