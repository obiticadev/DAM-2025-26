# EJERCICIO 12.05 — Integrador: edición + terminal + scratch + sesión

> Teoría:   teoria/12_Terminal_Snacks_Sesiones.md (TODA)
> Verifica: bash scripts/verificar.sh 12 05

## Escenario

Vas a editar este archivo, ejecutar el verificador en una terminal embebida, guardar notas en un scratch, y al final dejar el layout listo para retomarlo mañana.

## TODOs

**TODO 1 (con pista):** Cambia `INTEGRADOR_PENDIENTE` por `INTEGRADOR_OK` en la sección REGISTROS. Guarda con `:w`.

**TODO 2 (con pista):** Abre la terminal con `<leader>ft`. Ejecuta `bash scripts/verificar.sh 12 05`. Verás el diff. Sustituye `VERIFICADOR_RUN` por `OK`.

**TODO 3 (con pista):** Abre un scratch con `<leader>.`. Escribe la siguiente nota literal: "Mañana revisar el nivel 13 (Java + jdtls)". Cierra el scratch. Sustituye `SCRATCH_NOTA` por `OK`.

**TODO 4 (LIBRE):** Sal de nvim con `:wqa`. Vuelve a abrir desde el mismo cwd y restaura sesión con `<leader>qs`. Sustituye `SESION_RECUPERADA` por `OK`.

**TODO 5 (LIBRE):** Guarda y sal.

---

REGISTROS
integrador: INTEGRADOR_OK
verificador: OK
scratch: OK
sesion: OK
