# EJERCICIO 12.03 — Scratch buffer

> Teoría:   teoria/12_Terminal_Snacks_Sesiones.md (sección 4)
> Verifica: bash scripts/verificar.sh 12 03

## TODOs

**TODO 1 (con pista):** Pulsa `<leader>.` para abrir un scratch buffer. Es un buffer sin archivo. Escribe algo como "estoy probando". Cierra el scratch con `<leader>.` otra vez. Sustituye `SCRATCH_OPEN` por `OK`.

**TODO 2 (con pista):** Abre el scratch de nuevo con `<leader>.` y comprueba que el texto que escribiste sigue allí (snacks persiste los scratch entre toggles). Sustituye `SCRATCH_PERSIST` por `OK`.

**TODO 3 (LIBRE):** Crea un scratch NUEVO con `<leader>S` (mayúscula). Verás que cada `<leader>S` te da un scratch limpio (independiente). Sustituye `SCRATCH_NUEVO` por `OK`.

**TODO 4 (LIBRE):** Guarda y sal.

---

REGISTROS
abierto: OK
persist: OK
nuevo: OK
