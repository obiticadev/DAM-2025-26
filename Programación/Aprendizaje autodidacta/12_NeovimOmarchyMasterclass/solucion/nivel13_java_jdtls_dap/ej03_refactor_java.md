# EJERCICIO 13.03 — Refactor con jdtls (rename + extract method + format)

> Teoría:   teoria/13_Java_JDTLS_DAP.md (sección 3)
> Verifica: bash scripts/verificar.sh 13 03

Sigue los TODOs dentro del archivo:
`ejercicios/nivel13_java_jdtls_dap/com/bootcamp/nivel13/Refactor.java`

## TODOs (de control)

**TODO 1 (con pista):** Confirma que `<leader>cr` renombró `precioOriginal` → `precio` en TODO el método. Sustituye `RENAME_VAR` por `OK`.

**TODO 2 (con pista):** Confirma que `<leader>cr` renombró `aplicarDescuento` → `descontar` en definición Y main. Sustituye `RENAME_METHOD` por `OK`.

**TODO 3 (con pista):** Confirma que `<leader>cR` extrajo el método `calcular`. Sustituye `EXTRACT_METHOD` por `OK`.

**TODO 4 (LIBRE):** Confirma que `<leader>cf` formateó el archivo con google-java-format. Sustituye `FORMAT` por `OK`.

**TODO 5 (LIBRE):** Guarda y sal.

---

REGISTROS
rename_var: OK
rename_method: OK
extract: OK
format: OK
