# EJERCICIO 13.02 — Navegación LSP en Java

> Teoría:   teoria/13_Java_JDTLS_DAP.md (sección 3)
> Verifica: bash scripts/verificar.sh 13 02

El archivo Java de este ejercicio está en:
`ejercicios/nivel13_java_jdtls_dap/com/bootcamp/nivel13/Calculadora.java`

## Procedimiento

Ábrelo con:
```bash
nvim ejercicios/nivel13_java_jdtls_dap/com/bootcamp/nivel13/Calculadora.java
```

Sigue los TODOs dentro del archivo `.java`.

Cuando termines (especialmente el TODO 4 — organize imports — que ELIMINA un import sin usar), guarda con `:w` y vuelve aquí.

## TODOs (de control)

**TODO 1 (con pista):** Confirma que pudiste hacer `gd` desde el main hasta `sumar`. Sustituye `GD_JAVA` por `OK`.

**TODO 2 (con pista):** Confirma que `K` te mostró el Javadoc de `List`. Sustituye `HOVER_LIST` por `OK`.

**TODO 3 (con pista):** Confirma que `<leader>co` borró el import sobrante. Sustituye `ORGANIZE_IMP` por `OK`.

**TODO 4 (LIBRE):** Guarda y sal.

---

REGISTROS
gd: OK
hover: OK
organize: OK
