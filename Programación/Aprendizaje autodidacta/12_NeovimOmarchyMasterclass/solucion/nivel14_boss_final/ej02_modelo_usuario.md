# EJERCICIO 14.02 — Implementar Usuario (modelo)

> Verifica: cd nivel14_boss_final && mvn test -Dtest=UsuarioTest

## TODOs (en archivos Java)

Edita: `src/main/java/com/bootcamp/finale/modelo/Usuario.java`.

Sigue los TODOs 1-6 del archivo. Cuando termines, ejecuta:

```bash
mvn test -Dtest=UsuarioTest
```

Debe estar TODO en verde (8 tests).

## TODO de control

**TODO 1 (LIBRE):** Cuando `mvn test -Dtest=UsuarioTest` esté completamente VERDE, sustituye `USUARIO_VERDE` (más abajo) por `OK`.

---

REGISTROS
usuario_verde: OK
