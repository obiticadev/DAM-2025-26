# EJERCICIO 12.04 — Sesiones avanzadas

> Teoría:   teoria/12_Terminal_Snacks_Sesiones.md (sección 5)
> Verifica: bash scripts/verificar.sh 12 04

## TODOs

**TODO 1 (con pista):** Estás en este archivo. Abre 3 buffers más usando el picker (`<leader><space>`): elige `teoria/12_*`, `INSTALL_BY_OS.md`, `README_GUIA_TERMINAL.md`. Crea un split horizontal con `:sp ejercicios/nivel00_introduccion/ej01_abrir_y_salir.txt`. Sustituye `LAYOUT_OK` por `OK`.

**TODO 2 (con pista):** Sal con `:qa`. Vuelve a abrir nvim desde el mismo cwd (`cd 01_NeovimOmarchyMasterclass && nvim`). Pulsa `<leader>qs` (restore session). Deberías ver el layout y los 4-5 buffers de antes. Sustituye `RESTORE_OK` por `OK`.

**TODO 3 (LIBRE):** Ejecuta `:lua require("persistence").select()` (si tu config lo expone) para ver TUS sesiones guardadas. Pulsa Esc. Sustituye `PERS_LIST` por `OK`.

**TODO 4 (LIBRE):** Guarda y sal.

---

REGISTROS
layout: LAYOUT_OK
restore: RESTORE_OK
list: PERS_LIST
