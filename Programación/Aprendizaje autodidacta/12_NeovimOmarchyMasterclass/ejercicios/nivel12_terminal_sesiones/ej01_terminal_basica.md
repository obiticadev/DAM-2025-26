# EJERCICIO 12.01 — Terminal embebida básica

> Teoría:   teoria/12_Terminal_Snacks_Sesiones.md (sección 2)
> Verifica: bash scripts/verificar.sh 12 01

## TODOs

**TODO 1 (con pista):** Pulsa `<leader>ft` para abrir la terminal flotante de snacks. Escribe `echo "hola desde nvim"` + Enter. Sustituye `TERM_OPEN` por `OK`.

**TODO 2 (con pista):** Sin cerrar la terminal, pulsa `<C-/>` para OCULTARLA (no la matas, solo la escondes). Pulsa `<C-/>` otra vez para mostrarla — el comando anterior sigue allí. Sustituye `TERM_TOGGLE` por `OK`.

**TODO 3 (con pista):** Dentro de la terminal, pulsa `<C-\><C-n>` para pasar a modo Normal de la terminal. Navega con `k` hacia arriba para ver el output de `echo`. Pulsa `i` para volver al modo Terminal. Sustituye `TERM_NORMAL` por `OK`.

**TODO 4 (LIBRE):** Ejecuta `ls -la` dentro de la terminal flotante. Cuenta cuántas líneas tiene la salida (incluida la línea de "total ..."). Sustituye `LS_LINES` por el número (la solución usa `10`).

**TODO 5 (LIBRE):** Cierra la terminal completamente con `exit` (mata el shell). Vuelve a este archivo. Guarda y sal.

---

REGISTROS
abierto: TERM_OPEN
toggle: TERM_TOGGLE
normal_mode: TERM_NORMAL
ls_lineas: LS_LINES
