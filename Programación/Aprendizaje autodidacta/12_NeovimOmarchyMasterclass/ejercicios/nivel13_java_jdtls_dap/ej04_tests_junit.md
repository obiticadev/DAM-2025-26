# EJERCICIO 13.04 — Tests JUnit desde nvim con java-test

> Teoría:   teoria/13_Java_JDTLS_DAP.md (sección 4)
> Verifica: bash scripts/verificar.sh 13 04

## Procedimiento

```bash
nvim ejercicios/nivel13_java_jdtls_dap/com/bootcamp/nivel13/CalculadoraTest.java
```

## TODOs

**TODO 1 (con pista):** Pon el cursor DENTRO del método `sumarDosMasTres`. Pulsa `<leader>tt` (run nearest test). Espera. Aparece resultado verde. Sustituye `RUN_NEAREST` por `OK`.

**TODO 2 (con pista):** Pulsa `<leader>tT` (run all tests del archivo). Los 3 tests pasan. Sustituye `RUN_ALL` por `OK`.

**TODO 3 (LIBRE):** Alternativa terminal — abre `<leader>ft` y ejecuta `mvn test -Dtest=CalculadoraTest`. Comprueba que pasan. Sustituye `MVN_TEST` por `OK`.

**TODO 4 (LIBRE):** Guarda y sal.

---

REGISTROS
run_nearest: RUN_NEAREST
run_all: RUN_ALL
mvn_test: MVN_TEST
