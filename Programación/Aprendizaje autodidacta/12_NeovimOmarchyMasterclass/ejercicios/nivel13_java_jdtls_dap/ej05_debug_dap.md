# EJERCICIO 13.05 — Debugger con DAP

> Teoría:   teoria/13_Java_JDTLS_DAP.md (sección 5)
> Verifica: bash scripts/verificar.sh 13 05

Sigue los TODOs dentro de:
`ejercicios/nivel13_java_jdtls_dap/com/bootcamp/nivel13/Debug.java`

## TODOs (de control)

**TODO 1 (con pista):** Confirma que pudiste poner breakpoint con `<leader>db`. Sustituye `BREAKPOINT` por `OK`.

**TODO 2 (con pista):** Confirma que `<leader>dc` lanzó la sesión y paró en el breakpoint. Sustituye `LAUNCH` por `OK`.

**TODO 3 (con pista):** Confirma que `<leader>du` te mostró las variables. Sustituye `DAP_UI` por `OK`.

**TODO 4 (LIBRE):** Confirma que encontraste el bug (`i < n` → `i <= n`) y `factorial(5)` ahora devuelve `120`. Sustituye `BUG_ARREGLADO` por `OK`.

**TODO 5 (LIBRE):** Guarda y sal.

---

REGISTROS
breakpoint: BREAKPOINT
launch: LAUNCH
ui: DAP_UI
bug: BUG_ARREGLADO
