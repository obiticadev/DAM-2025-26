# EJERCICIO 14.01 — Abrir proyecto y compilar (rojo esperado)

> Teoría:   teoria/14_Boss_Final_Java_IDE.md (todo)
> Verifica: bash scripts/verificar.sh 14   (cuando TODO esté implementado)

## TODOs

**TODO 1:** Desde la raíz del bootcamp:
```bash
cd ejercicios/nivel14_boss_final
nvim .
```
Pulsa `<leader>e` y verifica que ves la estructura `src/main` y `src/test`. Sustituye `ABIERTO` por `OK`.

**TODO 2:** Pulsa `<leader>ft` (terminal) y ejecuta `mvn clean test`. Espera. Verás MUCHOS errores rojos: las clases `Usuario`, `UsuarioRepositorio`, `UsuarioServicio` no implementan los TODOs. **Eso es lo esperado.** Sustituye `COMPILE_ROJO` por `OK`.

**TODO 3 (LIBRE):** Abre `src/main/java/com/bootcamp/finale/modelo/Usuario.java` con `<Space><Space>` + `Usuario` + Enter. Lee los TODOs del archivo. NO los implementes todavía. Sustituye `LEI_USUARIO` por `OK`.

**TODO 4 (LIBRE):** Guarda y sal.

---

REGISTROS
abierto: ABIERTO
compile_rojo: COMPILE_ROJO
lei_usuario: LEI_USUARIO
