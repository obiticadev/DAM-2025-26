# EJERCICIO 11.03 — lazygit: workflow visual completo

> Teoría:   teoria/11_Git_Gitsigns_LazyGit_Trouble.md (sección 3)
> Verifica: bash scripts/verificar.sh 11 03

## Pre-requisito

`lazygit` instalado (ver INSTALL_BY_OS.md).

## TODOs

**TODO 1 (con pista):** Modifica este archivo: cambia `LAZYGIT_PENDIENTE` por `LAZYGIT_LISTO`. Guarda.

**TODO 2 (con pista):** Pulsa `<leader>gg` para abrir lazygit. Se abre el panel TUI. Pulsa `?` para ver la ayuda. Pulsa `q` para cerrar la ayuda. Sustituye `LAZYGIT_ABIERTO` por `OK`.

**TODO 3 (con pista):** Dentro de lazygit, navega al panel "Files" (pulsa `2`). Verás este archivo modificado. Pulsa `<Space>` sobre él para stagearlo. Sustituye `STAGE_LZG` por `OK`.

**TODO 4 (LIBRE):** Pulsa `c` para commitear. Te abre un editor para el mensaje. Escribe `practica: ejercicio 11.03 lazygit` y guarda+sal (`:wq`). El commit se hace. Sustituye `COMMIT_OK` por `OK`. 

**TODO 5 (LIBRE):** En lazygit, navega al panel "Commits" (pulsa `4`). Deberías ver tu nuevo commit en el TOP. Pulsa `q` para salir de lazygit. Sustituye `VISTO_COMMIT` por `OK`.

**TODO 6 (LIBRE):** Guarda y sal.

> ⚠️ Si NO quieres ensuciar el historial git con commits de práctica, salta los TODOs 4 y 5 y rellena con `SKIP`.

---

REGISTROS
abierto: LAZYGIT_ABIERTO
stage: STAGE_LZG
commit: COMMIT_OK
visto: VISTO_COMMIT
estado: LAZYGIT_PENDIENTE
