# EJERCICIO 14.05 — Refactor final + commit con lazygit

> Verifica: bash scripts/verificar.sh 14   (mvn clean test global)

## TODOs

**TODO 1 (LIBRE):** Formatea TODOS los archivos Java de `src/main/java/...` con `<leader>cf` (necesita `google-java-format` instalado vía Mason). Guarda.

**TODO 2 (LIBRE):** Usa `mini.surround` en al menos UN string: convierte algún `"texto"` a `'texto'` con `gsr"'`, y vuelve a `"` con `gsr'"`. Solo para practicar la herramienta. Sustituye `MINI_SURROUND_USADO` por `OK`.

**TODO 3 (LIBRE):** Ejecuta el verificador final:
```bash
bash scripts/verificar.sh 14
```
Debe imprimir ✅. Si no, vuelve a los ejercicios pendientes. Sustituye `VERIFICADOR_VERDE` por `OK`.

**TODO 4 (LIBRE):** Abre lazygit con `<leader>gg`, stagea TODOS los cambios (`a`), commit con mensaje `boss-final: implementacion completa nivel 14`. Sustituye `COMMIT_FINAL` por `OK`.

**TODO 5 (LIBRE):** Sustituye `BOSS_FINAL_COMPLETADO` por `OK`. Has terminado el bootcamp. Guarda y sal.

---

REGISTROS
mini_surround: MINI_SURROUND_USADO
verificador: VERIFICADOR_VERDE
commit: COMMIT_FINAL
boss_final: BOSS_FINAL_COMPLETADO
