Siguiendo la misma filosofía de estructuración lógica, visual y profesional de las entregas anteriores, aquí tienes el análisis y organización del contenido de este nuevo documento sobre **Funciones en SQL (enfocado en Oracle)**.

---

# 📘 SQL Cheatsheet: Funciones y Operaciones

## 1. Concepto de Función
Las funciones son operaciones predefinidas que se ejecutan sobre los datos para realizar cálculos o transformaciones. 

*   **Entrada/Salida:** Reciben valores de entrada (**parámetros** o argumentos) y devuelven un único resultado basado en el cálculo realizado.
*   **Sintaxis General:**
    ```sql
    NombreFunción ([parámetro1], [parámetro2], ...)
    ```
*   **Anidamiento:** Es posible incluir una función dentro de otra, de modo que el resultado de la función interna sirva como parámetro para la externa.

---

## 2. Aplicación en Consultas SQL
Las funciones no son comandos aislados, sino que se integran dentro de las cláusulas estándar de una sentencia `SELECT`.

| Cláusula | Propósito de la función |
| :--- | :--- |
| **SELECT** | Para mostrar un dato transformado o calculado en el resultado final. |
| **WHERE** | Para filtrar registros basados en una transformación (ej. buscar en mayúsculas). |
| **ORDER BY** | Para ordenar los resultados según el valor devuelto por la función. |

---

## 3. Clasificación de Funciones
Dependiendo del tipo de dato sobre el que actúan y el resultado que ofrecen, se dividen en los siguientes grupos:

### A. Funciones de Fila Única
Actúan sobre cada fila de la consulta de forma individual.
1.  **Numéricas:** Realizan cálculos matemáticos (ej. redondeos, potencias).
2.  **De Cadena (Caracteres):** Modifican texto (ej. pasar a mayúsculas, recortar espacios, concatenar).
3.  **De Manejo de Fechas:** Operan con el calendario (ej. sumar meses, calcular días entre fechas).
4.  **De Conversión:** Cambian el tipo de dato (ej. transformar un número o fecha en una cadena de texto formateada).
5.  **Otras:** Funciones especiales de control o de sistema.

---

## 4. Herramientas de Prueba: La Tabla `DUAL`
En Oracle, para probar funciones o realizar cálculos rápidos sin necesidad de consultar una tabla de negocio real, se utiliza la tabla **DUAL**.

*   **¿Qué es?** Una tabla especial creada automáticamente por el sistema.
*   **Estructura:** Tiene una sola columna llamada `DUMMY` y una sola fila.
*   **Utilidad:** Ideal para ejecutar funciones que no dependen de datos de usuario.

**Ejemplo de uso:**
```sql
-- Para ver la fecha actual del sistema
SELECT SYSDATE FROM DUAL;

-- Para probar una función de texto
SELECT UPPER('hola mundo') FROM DUAL;
```

---

## 💡 Guía de Aprendizaje Lógico
Para dominar este módulo, el orden correcto de estudio es:
1.  **Entender la Sintaxis:** Comprender que toda función lleva paréntesis, incluso si no tiene parámetros.
2.  **Experimentar con DUAL:** Antes de aplicar funciones a tablas complejas, probar su comportamiento en la tabla `DUAL`.
3.  **Dominar el Anidamiento:** Aprender a leer de "dentro hacia afuera" cuando veas varias funciones juntas.
4.  **Casos de Uso por Cláusula:** Practicar primero usando funciones en el `SELECT` (para visualizar) y luego en el `WHERE` (para filtrar).