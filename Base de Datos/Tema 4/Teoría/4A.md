Esta es la **Cheatsheet Profesional de SQL: Recuperación de Datos (DQL)**, organizada de manera lógica para un aprendizaje progresivo, desde la estructura básica hasta la lógica de precedencia de operadores.

---

# 📘 SQL Cheatsheet: Realización de Consultas (SELECT)

## 1. Estructura Básica de una Consulta
La sentencia `SELECT` es el comando fundamental para recuperar información de una o varias tablas.

**Sintaxis General y Orden de Cláusulas:**
```sql
SELECT [ALL | DISTINCT] columna1, columna2...
FROM tabla1, tabla2...
[WHERE condición]
[ORDER BY columna [ASC | DESC]];
```

### Cláusulas Principales:
*   **SELECT:** Define qué columnas queremos ver.
    *   `ALL`: Muestra todas las filas (valor por defecto).
    *   `DISTINCT`: Elimina filas duplicadas del resultado.
    *   `*`: Comodín para mostrar **todas** las columnas de la tabla.
*   **FROM:** Especifica la tabla o tablas de origen.
*   **WHERE:** Filtra las filas que cumplen una condición específica (opcional).
*   **ORDER BY:** Ordena los resultados (opcional).

---

## 2. Identificación y Alias
Para mejorar la legibilidad o resolver ambigüedades:

*   **Nombres Cualificados:** `NombreTabla.NombreColumna` (útil en consultas multitabla).
*   **Alias de Columna:** Se usa para cambiar el nombre de la cabecera en el resultado.
    ```sql
    SELECT F_Nacimiento AS "Fecha de Nacimiento" FROM USUARIOS;
    ```
*   **Alias de Tabla:** Se definen en el `FROM` para abreviar.
    ```sql
    SELECT U.Nombre FROM USUARIOS U;
    ```
*   **Propietarios:** En entornos multiusuario, se puede especificar el esquema: `USUARIO.TABLA`.

---

## 3. Operadores de Filtrado (Cláusula `WHERE`)
Permiten extraer solo los registros que interesan.

### A. Comparación Relacional
| Operador | Significado |
| :--- | :--- |
| `=` | Igual a |
| `!=`, `<>`, `^=` | Distinto de |
| `>`, `<` | Mayor que / Menor que |
| `>=`, `<=` | Mayor o igual / Menor o igual |

### B. Operadores de Conjunto y Rango
*   **IN (valor1, valor2):** Comprueba si el dato coincide con cualquier elemento de la lista.
*   **BETWEEN min AND max:** Comprueba si el valor está dentro de un rango (incluyendo los límites).
*   **IS NULL / IS NOT NULL:** Verifica si un campo está vacío o no. *(Nota: No se puede usar `= NULL`)*.

### C. Coincidencia de Patrones (LIKE)
Se usa con cadenas de texto:
*   `%`: Sustituye a cualquier cadena de caracteres (cero o más).
*   `_`: Sustituye a un único carácter.
*   *Ejemplo:* `WHERE nombre LIKE 'A%'` (Nombres que empiezan por A).

---

## 4. Consultas Calculadas y Expresiones
Podemos realizar operaciones sobre la marcha sin modificar los datos originales.

*   **Aritmética:** `+`, `-`, `*`, `/`.
    *   *Importante:* Cualquier operación aritmética con un valor `NULL` da como resultado `NULL`.
*   **Concatenación (`||`):** Une varias columnas o textos en una sola columna de salida.
    ```sql
    SELECT Nombre || ' ' || Apellido1 FROM EMPLEADOS;
    ```

---

## 5. Lógica y Precedencia
Cuando combinamos varias condiciones, el orden de evaluación es crucial.

### Operadores Lógicos
1.  **NOT:** Invierte el valor de la condición.
2.  **AND:** Verdadero si AMBAS condiciones se cumplen.
3.  **OR:** Verdadero si AL MENOS UNA condición se cumple.

### Reglas de Precedencia (Orden de ejecución)
Para evitar errores en expresiones complejas, SQL sigue este orden:
1.  Paréntesis (prioridad máxima).
2.  Operadores aritméticos (`*`, `/` primero; luego `+`, `-`).
3.  Operador de concatenación (`||`).
4.  Operadores de comparación (`=`, `<>`, `LIKE`, `IN`, etc.).
5.  **NOT**.
6.  **AND**.
7.  **OR**.

---

## 6. Ordenación de Resultados (ORDER BY)
Por defecto, el orden de los registros es impredecible.

*   **ASC:** Orden ascendente (A-Z, 0-9). Es el valor por defecto.
*   **DESC:** Orden descendente (Z-A, 9-0).
*   **Ordenación Múltiple:** Se puede ordenar por varias columnas.
    ```sql
    ORDER BY Apellido ASC, Nombre ASC; -- Ordena por apellido, y si coinciden, por nombre.
    ```
*   **Por posición:** Se puede usar el número de columna del `SELECT`.
    ```sql
    SELECT nombre, apellido FROM USUARIOS ORDER BY 2; -- Ordena por 'apellido'.
    ```

---

## 💡 Ruta Lógica de Aprendizaje
1.  **Entender la estructura:** No puedes filtrar (`WHERE`) sin antes saber de dónde vienen los datos (`FROM`).
2.  **Dominar los tipos de datos:** Saber cuándo usar comillas (para texto y fechas) y cuándo no (números).
3.  **Cuidado con los NULLs:** Aprender que el `NULL` no es un cero ni un espacio, es un valor "desconocido".
4.  **Uso de paréntesis:** Ante la duda en condiciones `AND/OR`, usa siempre paréntesis para asegurar el resultado deseado.